This simple proxy currently only handles the GET method.

Strengths:

Simple
Fast

Weaknesses:

Only GET requests
HTTP/1.0 does not keep a persistent connection
Sites that auto redirect to https return a 301
